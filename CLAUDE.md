Never edit files without asking, if `accept edits` isn't turned on.

## Skill usage

- When doing anything related to `NextJS`, please use the `next-best-practices` skill.
- When doing anything related to `NextJS server actions`, please use the `next-safe-action` skill.
