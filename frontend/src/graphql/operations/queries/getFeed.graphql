query GetFeed($input: GetFeedRequestBody!) {
  getFeed(input: $input) {
    count
    posts {
      id
      imageURL
      description
      authorID
      authorProfilePreview {
        id
        name
        username
      }
    }
  }
}
