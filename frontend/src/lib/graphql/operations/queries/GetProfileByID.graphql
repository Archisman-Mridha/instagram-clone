query GetProfileByID($args: GetProfileByIDArgs!) {
  getProfileByID(args: $args) {
    id
    name
    username
    bio
    followshipCounts {
      followeeCount
      followerCount
    }
    isFollowee
    posts {
      ...PostFragment
    }
  }
}
