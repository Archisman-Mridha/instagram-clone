apiVersion: v1

## A Kubernetes ServiceAccount(SA) represents a non-human user. Application Pods, system
## components, and entities inside and outside the cluster can use it, to authenticate to the
## Kubernetes API server.
kind: ServiceAccount

metadata:
  name: cluster-admin

  ## Each service account is bound to a Kubernetes namespace.
  namespace: kubernetes-dashboard
